{
  "name": "set",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd back && npm i && npm install knex -g && knex migrate:latest && knex seed:run && node app.js\" \"cd views && npm i && npm run build && npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0",
    "pg": "^7.8.0"
  }
}
